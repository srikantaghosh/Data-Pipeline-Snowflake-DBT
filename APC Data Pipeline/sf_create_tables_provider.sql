

-- Create table for provider1 raw data
CREATE OR REPLACE TABLE provider1_raw (
    provider_id STRING,
    player_name STRING,
    first_name STRING,
    last_name STRING,
    date_of_birth DATE,
    gender STRING,
    country STRING,
    nickname STRING,
    jersey_number STRING,
    nationality STRING
);

-- Create table for provider2 raw data
CREATE OR REPLACE TABLE provider2_raw (
    provider_id STRING,
    player_name STRING,
    first_name STRING,
    last_name STRING,
    date_of_birth DATE,
    gender STRING,
    country STRING,
    nickname STRING,
    jersey_number STRING,
    nationality STRING
);

-- Create table for provider3 raw data
CREATE OR REPLACE TABLE provider3_raw (
    provider_id STRING,
    player_name STRING,
    first_name STRING,
    last_name STRING,
    date_of_birth DATE,
    gender STRING,
    country STRING
);
